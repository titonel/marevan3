version: '3.8'

services:
  web:
    build: .
    container_name: marevan_app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
    stdin_open: true
    tty: true